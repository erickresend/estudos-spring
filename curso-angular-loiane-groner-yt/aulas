## Aula 07
Foi falado sobre a divisao entre component e service, que o component é usado para tratar a manipulação da página e o service para a logica de acesso ao banco/api. Injeção de dependencia do service via construtor do component podendo assim retornar a logica passada, e nao esquecer de adicionar no providers do modulo, caso nao tenha tem que criar.

## Aula 08
Foi só sobre pluguins

## Aula 09
Aula sobre Data Binding, que seria a comunicação entre o templete e a logica
Interpolação que é usar {{ conteudo }} dentro do html
Property binding que é colocar a propriedade dentro [] ex: <img [src]="urlImagem">
